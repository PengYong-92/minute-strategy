# 自适应常驻画像与持续影子优化设计

## 1. 目标与边界

将当前“最近7天每日重新入选”的画像机制改造成7天快速入选、14天稳定保留和N12/N20即时状态组成的自适应画像。程序自动决定画像的进入、保留、降级、暂停、恢复和退出，不写死人工画像名单。

本功能只处理完整观察画像键：

```text
10分钟 | strategy_family | strategy_tag | LONG/SHORT | WD/WE-UTC小时
```

影子验证期间不直接修改当前`CHAMPION`的10分钟候选生成、评分、方向判断、总并发、方向并发、2分钟同方向间隔、10U/18U金额、滚单规则、Webhook和其他既有守卫。候选参数只能在独立`CHALLENGER`账本中生效；通过第10节升级门槛后，冻结的新版本才影响后续正式订单。现有方向脉冲继续保持影子模式，现有24小时方向健康守卫保持原逻辑。

本设计同时整合“10分钟入场价格结构确认”。价格结构第一阶段固定为`SHADOW_ONLY`，只记录当前候选与支撑、压力、整数关口、突破、回踩和假突破的关系，不改变正式开单。自适应画像是本阶段唯一新增的正式准入变化。

在上述基础上增加长期`Champion-Challenger`影子优化系统。当前正式参数作为`CHAMPION`，最多7组冻结候选参数作为`CHALLENGER`，使用同一批已闭合1分钟K线、各自独立的虚拟订单和状态账本进行前向验证。影子策略在独立CPU进程和独立`monitor.shadow.sqlite3`中运行，不发送Webhook、不写入正式订单、不占用正式容量，也不得增加正式信号关键路径延迟；只有满足第10节全部前向门槛的候选才允许成为下一版主策略。

## 2. 数据窗口评估

### 2.1 数据范围

线上SQLite保留全部观察记录，没有按7天删除。只读导出的BTCUSDT观察数据为：

- 8513条观察记录；
- 8511条已结算记录；
- 时间范围为2026-07-17至2026-08-16；
- 总跨度约30.06天。

因此“7天”只是当前每日画像的计算窗口，不是数据库保留期限。观察画像历史不按固定天数删除；本设计只调整必要指标和高频审计写入方式。

### 2.2 逐日前向比较

对每个北京时间07:50评估点只使用当时已经结算的独立观察样本，画像在08:00生效，再统计下一生效周期的独立观察结果。共同日期使用相同日期比较，避免不同样本期造成偏差。

| 窗口 | 共同评估天数 | 日均入选画像 | 次日留存率 | 日均新增/移除 | 前向观察数 | 前向胜率 | 前向EV |
|---|---:|---:|---:|---:|---:|---:|---:|
| 7天 | 16 | 20.44 | 80.12% | 4.53 / 4.20 | 897 | 50.72% | -0.87U |
| 14天 | 16 | 14.69 | 85.16% | 2.40 / 2.27 | 589 | 51.10% | -0.80U |

在仅有9个共同评估日的补充比较中，21天窗口日均入选9.78个、留存率91.05%、前向胜率53.43%，但覆盖率仅8.50%，且有效评估日不足，不能据此上线21天或30天窗口。30天没有完整的前向评估周期。

14天窗口相对7天窗口只将前向胜率提高0.38个百分点，却将前向观察机会减少34.34%。前向观察不是正式订单，不能直接等同订单减少比例，但足以证明“14天单窗替换7天单窗”不符合胜率和订单量双优先目标。

结论：7天继续负责快速发现当前有效画像，14天只负责稳定保留，不能成为唯一入选门槛。近期退化由完整画像键的N12/N20联合状态处理。正式实施前必须同时满足第3.4节的胜率和订单保留验收线。

## 3. 总体架构

### 3.1 稳定资格层

每日北京时间07:50同时计算最近7天和14天已结算独立观察样本，08:00生效。

两个窗口均使用现有程序阈值：

- 工作日最少20笔独立样本；
- 周末最少10笔独立样本；
- 总胜率不低于60%；
- 总EV大于等于0；
- 完整画像键隔离计算；
- 同一完整键内重叠的10分钟观察只计一次。

资格采用“快速进入、稳定退出”：

- 未启用画像只要7天窗口本次达标，立即成为`QUALIFIED`，不等待第二次日评估；
- 已启用画像只要7天或14天任一窗口达标，保持`QUALIFIED`并将联合失效次数归零；
- 两个窗口本次均不达标时标记`QUALIFICATION_WATCH`，继续保留并将联合失效次数加一；
- 两个窗口连续2次日评估均不达标后退出正式资格；
- 退出后继续记录观察，任一后续7天窗口重新达标即可恢复资格。

7天、14天统计和联合失效次数保存在每日快照的全部`candidates`中，而不只保存在`selected_profiles`中；服务重启后从最近一份每日快照恢复，不能因为重启丢失第一次联合失效记录。

首次升级到本设计且没有双窗口快照时，当前正在生效的7天画像全部作为`QUALIFIED`迁入，联合失效次数设为0，不因迁移形成空窗或等待期。

资格层不使用人工画像白名单。所有10分钟观察画像均可参与计算。

### 3.2 即时自适应状态层

每个完整画像键独立维护最近12笔和最近20笔非重叠观察结果。每次该画像有独立观察结算后立即重算，不等待07:50、整点或4小时边界。

状态规则：

| 状态 | 条件 | 正式开单行为 |
|---|---|---|
| `WARMUP` | 最近独立样本少于12笔 | 已有稳定资格时保持原行为 |
| `ACTIVE` | N12至少7胜，且N20不足20笔或N20 EV大于等于0 | 正常遵循现有开单与滚单规则 |
| `WATCH` | 已满12笔但不满足`ACTIVE`，且不同时满足`PAUSED`的两个条件 | 保留10U首单，禁止现有`DIRECTION_V2`方向第二席位和18U滚单 |
| `PAUSED` | N12最多5胜，并且N20已满20笔且EV小于0 | 阻止该画像正式开单，但继续记录观察 |

恢复使用天然迟滞：

- `PAUSED`在N12回到至少6胜，或N20 EV恢复非负后进入`WATCH`；
- `WATCH`只有在N12达到至少7胜，且N20不足20笔或N20 EV恢复非负后进入`ACTIVE`；
- 不按一笔输赢直接从`PAUSED`跳到`ACTIVE`。

N12使用离散胜场而不是近似百分比，避免边界浮点歧义：7/12为58.33%，高于10U赢8U、输10U对应的55.56%盈亏平衡胜率；6/12为50%，只能作为观察状态。N20不足时不得仅凭N12最多5胜直接停掉全部首单，避免小样本把订单量一次性压低。

### 3.3 准入顺序

保持现有候选选择和既有守卫顺序，只在每日画像资格命中后增加完整画像状态检查：

1. 生成原有10分钟候选和观察候选；
2. 检查7天快速入选和14天稳定保留资格层；
3. 附加完整画像即时状态；
4. `PAUSED`返回`ADAPTIVE_PROFILE_PAUSED`；
5. `WATCH`候选处于现有`DIRECTION_V2`方向第二席位时，返回`ADAPTIVE_PROFILE_SECOND_BLOCKED`；
6. `WATCH`首单继续进入既有机械准入，但金额固定为10U且不消费18U资格；
7. `ACTIVE/WARMUP`进入既有准入和守卫；
8. 无论是否正式开单，观察候选继续记录和结算。

完整画像状态只会收紧已经取得稳定资格的画像，不会启用未取得资格的画像、改变方向或把WAIT直接提升为订单。

### 3.4 胜率和订单量双验收线

实现后必须以当前7天生产逻辑为同区间因果基准，逐笔比较完整画像入选、候选、正式订单和金额资格。方案只有同时满足以下条件才具备发布资格：

1. 样本外总胜率不低于60%，LONG和SHORT分别不低于55.56%；
2. 总正式订单数至少保留基准的80%；
3. LONG和SHORT各自订单数至少保留各自基准的70%，不能通过停掉一个方向制造总胜率；
4. 10U首单数至少保留基准的85%，不能主要依靠关闭第二席位制造表面胜率；
5. 总EV、LONG EV和SHORT EV均大于等于0；
6. 最大回撤和最长连亏均不劣于基准；
7. 至少三分之二样本外滚动窗口EV为正；
8. 多组参数同时达标时，先按样本外胜率排序，再按订单保留数排序，最后比较最大回撤。

任一硬条件未满足时不得通过降低订单保留线或合并多空统计强行发布。价格结构影子数据不参与本轮正式准入结果。

## 4. 入场价格结构影子层

### 4.1 目标和不可偏离边界

价格结构层回答“当前候选的入场位置是否支持该方向”，不预测4小时或日线趋势，也不自行生成方向。它与画像机制各自回答不同问题：

- 7天快速入选与14天稳定保留层：这个完整画像是否具备启用资格；
- N12/N20即时状态层：这个画像近期是否退化；
- 入场价格结构影子层：这一笔候选在当前价格位置是否得到支撑、压力、突破或回踩确认。

第一阶段必须满足以下不变量：

1. 只使用当前时点已经闭合的1分钟K线；
2. 不增加30分钟、4小时、日线或外部趋势投票；
3. 不把结构状态加入完整画像键，避免拆散画像样本；
4. 不把结构结果计入7天/14天资格、N12/N20状态、动态评分或阈值；
5. 不修改`open_allowed`、候选方向、订单金额、方向席位、冷却、滚单或Webhook；
6. 不恢复历史上长期为负的120分钟假突破策略，结构层不能在没有现有候选时创建反向订单；
7. 影子开启前后的订单ID、方向、入场时间和金额必须完全一致。

### 4.2 组件和数据流

实现拆分为三个独立组件：

- `StructureDetector`：只根据已闭合1分钟K线识别确认摆动点、重复触及价格区和整数关口；
- `StructureStateMachine`：只维护接近、拒绝、突破待确认、突破确认、回踩、假突破和失效状态；
- `EntryStructureGate`：只把结构状态与候选方向、候选来源映射为`CONFIRMED/CONFLICT/PENDING/NEUTRAL`。第一阶段该结果仅为影子判断。

固定数据流为：

```text
已闭合1分钟K线
 -> 原有10分钟信号和观察候选
 -> StructureDetector
 -> StructureStateMachine
 -> EntryStructureGate(SHADOW_ONLY)
 -> 附加同一时点结构快照
 -> 自适应画像资格和即时状态
 -> 原有容量、冷却、风险守卫、金额资格与原子开单
```

三个结构组件不得读取订单结果、画像历史胜率、每日画像未来状态、滚单资格或未闭合K线。`MonitorState`只负责编排、持久化和展示。

### 4.3 因果结构识别V1

V1使用版本`ENTRY_STRUCTURE_SHADOW_V1`，固定采用下列因果口径收集线上样本：

- 历史窗口：最近240根已经闭合的1分钟K线；
- 波动尺度：以当前最后一根闭合K线计算的14周期1分钟ATR；
- 摆动点：左右各3根闭合K线，只有右侧第3根闭合后才确认中间摆动点；
- 低点只参与支撑聚类，高点只参与压力聚类；同类摆动价格的最大值与最小值差不超过0.25 ATR时属于同一区，区间边界就是该组摆动价格的最小值和最大值；
- 有效价格区至少包含2个已确认同类摆动点，两个摆动点至少间隔5根1分钟K线；普通K线高低区间与价格区相交只计为触及证据，不能替代已确认摆动点；
- 接近距离：结构边界0.35 ATR以内；
- 拒绝确认：K线高低区间与价格区相交后，支撑要求收盘高于上边界0.05 ATR，压力要求收盘低于下边界0.05 ATR；
- 突破缓冲：向上突破压力要求收盘高于上边界0.10 ATR，向下突破支撑要求收盘低于下边界0.10 ATR；
- 突破确认：连续2根闭合1分钟K线保持在结构外；
- 回踩观察：突破确认后的5根闭合1分钟K线；向上突破回踩与原区相交后收盘重新高于上边界0.05 ATR为守住，向下突破镜像处理；
- 假突破：向上突破待确认或确认后5根内收盘重新回到上边界以内，向下突破镜像处理；
- 结构失效：支撑连续3根收盘低于下边界0.35 ATR，或压力连续3根收盘高于上边界0.35 ATR。

同类型存在多个有效价格区时，按当前收盘价到区间最近边界的ATR距离分别选择最近支撑和最近压力；距离相同时依次按摆动点数量和最近确认时间排序。当前价格位于价格区内部时距离为0。状态机只追踪这两个最近结构及最近5根突破/回踩状态，使用同一240根闭合K线可在重启后确定性重建，不依赖订单或结算结果。

历史不足20根、ATR不可计算或ATR不大于0时输出`INSUFFICIENT_DATA/NEUTRAL`，不得阻断原流程。

BTC整数候选步长为100、500、1000，ETH为10、50、100。整数关口只有满足下列任一条件才成为有效结构来源：

1. 与有效摆动价格区距离不超过0.25 ATR，来源标记为`MERGED`；
2. 在240根窗口内按上述拒绝定义出现至少2次间隔5根以上的独立拒绝反应。

单纯接近整数价格不能获得`CONFIRMED`，也不能直接触发反向判断。所有距离同时计算绝对价格、基点和ATR倍数，状态判断只使用ATR倍数。

V1参数是影子样本的固定版本，不代表最优值。参数变更必须产生新版本，旧观察不得用新参数回算覆盖。

### 4.4 结构状态与方向映射

状态机至少输出：

| 状态 | 含义 |
|---|---|
| `INSUFFICIENT_DATA` | 闭合K线或ATR不足，结构层保持中性 |
| `NO_NEARBY_LEVEL` | 0.35 ATR内没有有效结构 |
| `APPROACHING_SUPPORT` | 接近支撑但尚未出现拒跌 |
| `APPROACHING_RESISTANCE` | 接近压力但尚未出现受阻 |
| `SUPPORT_REJECTED` | 触及支撑后重新收回支撑上方 |
| `RESISTANCE_REJECTED` | 触及压力后重新收回压力下方 |
| `BREAKOUT_PENDING` | 收盘已越过结构，但确认根数不足 |
| `BREAKOUT_CONFIRMED` | 满足缓冲并连续2根收盘站在结构外 |
| `RETEST_PENDING` | 确认突破后返回原结构附近，尚未证明守住 |
| `RETEST_HELD` | 回踩后重新收在突破方向一侧 |
| `FALSE_BREAKOUT` | 突破待确认或已确认后，在回踩窗口内重新收回结构内或反侧 |
| `LEVEL_INVALIDATED` | 结构已按失效规则持续穿越 |

方向映射固定为：

| 当前结构 | LONG影子判断 | SHORT影子判断 |
|---|---|---|
| 接近未突破压力 | `CONFLICT` | `NEUTRAL` |
| 压力拒绝 | `CONFLICT` | `CONFIRMED` |
| 向上突破待确认 | `PENDING` | `CONFLICT` |
| 向上突破确认或回踩守住 | `CONFIRMED` | `CONFLICT` |
| 接近未跌破支撑 | `NEUTRAL` | `CONFLICT` |
| 支撑拒跌 | `CONFIRMED` | `CONFLICT` |
| 向下突破待确认 | `CONFLICT` | `PENDING` |
| 向下突破确认或回踩守住 | `CONFLICT` | `CONFIRMED` |
| 假向上突破 | `CONFLICT` | `CONFIRMED` |
| 假向下突破 | `CONFIRMED` | `CONFLICT` |
| 数据不足、无附近结构或结构失效 | `NEUTRAL` | `NEUTRAL` |

候选来源必须单独记录：

- `NATIVE_ACTIONABLE`：原生实时LONG/SHORT且已达到自身动态阈值；
- `PROFILE_PROMOTED_WAIT`：原始实时信号未成立，但被每日画像提升；
- `RESEARCH_OBSERVATION`：仅观察候选。

同一个结构快照可以分别映射LONG和SHORT，但不能因此生成原本不存在的候选。

`candidate_origin`必须在每日画像对信号进行提升前确定：原始方向已是LONG/SHORT且原始绝对评分达到动态阈值时为`NATIVE_ACTIONABLE`；原始信号未达到该条件、随后因每日画像成为正式候选时为`PROFILE_PROMOTED_WAIT`；始终只观察时为`RESEARCH_OBSERVATION`。

当最近支撑和最近压力对同一候选产生不同判断时，最终`entry_structure_bias`按胜率优先的保守顺序选择：`CONFLICT > PENDING > CONFIRMED > NEUTRAL`。`active_level_*`保存产生最终判断的结构；同一优先级有多个结构时依次选择ATR距离更近、触及数更多、确认时间更新的结构。支撑和压力各自状态仍保留在最近结构摘要中，不能因为最终判断只保存一侧而丢失另一侧的距离和边界。

### 4.5 记录、展示和统计

结构快照必须在候选开单判断前生成，并原样附加到信号、观察样本、实际订单入口快照和订单API。不得在订单结算后根据后续K线重算。必要字段为：

```text
entry_structure_version
entry_structure_mode
entry_structure_evaluated_at
entry_structure_state
entry_structure_bias
entry_structure_reason_code
candidate_origin
active_level_source
active_level_lower/upper
active_level_touch_count
active_level_confirmed_at
nearest_support_lower/upper
nearest_resistance_lower/upper
support_distance_price/bps/atr
resistance_distance_price/bps/atr
breakout_direction
breakout_closed_bars
breakout_buffer_atr
retest_status
round_level_price/step
```

只保存当前候选的最近有效支撑、最近有效压力和决定性状态，不在每条信号中重复保存240根K线、全部历史价区或完整结构状态表。完整运行参数由6.5节的`runtime_config_hash`去重引用；正式结构证据留在正式库，影子结构证据留在5GiB影子库，不跨库重复整份载荷。

页面“最新K线分析”只增加一组紧凑结构标签；订单列表增加“价格结构”列；观察画像统计增加结构状态、影子判断和候选来源筛选。不得新增第二行重复分析卡。

观察统计必须按以下维度输出样本数、胜率、PnL和EV：

- `CONFIRMED/CONFLICT/PENDING/NEUTRAL`；
- LONG/SHORT；
- `NATIVE_ACTIONABLE/PROFILE_PROMOTED_WAIT/RESEARCH_OBSERVATION`；
- 摆动点、整数关口和`MERGED`来源；
- 支撑拒绝、压力拒绝、突破确认、回踩守住、假突破和无附近结构；
- 完整画像键及其`ACTIVE/WATCH/PAUSED/WARMUP`状态。

### 4.6 后续升级条件

影子结构不得因少量线上样本自动升级为真实门禁。只有完成因果回放并同时满足以下条件，才允许另行提出真实拦截设计：

1. 影子模式与未开启结构层的基准订单完全一致；
2. 样本外允许组总胜率不低于60%，LONG和SHORT分别不低于55.56%，两方向EV均大于0；
3. `PROFILE_PROMOTED_WAIT`的`CONFIRMED`组胜率至少比其余结构组高3个百分点，且不是单一时段贡献；
4. 未来真实门禁方案总订单至少保留基准80%，LONG和SHORT分别至少保留各自基准70%；
5. 最大回撤和最长连亏不劣于基准；
6. 至少三分之二样本外滚动窗口EV为正；
7. BTC和ETH分别满足条件，不互相掩盖；
8. 任何真实拦截都必须单独设计、单独确认，不得在本分支影子阶段暗中启用。

## 5. 时间段展示

画像键中的`WD/WE-xx`使用UTC日期类型和UTC小时。页面和API必须同时输出：

- 原始`threshold_segment`，例如`WD-18`；
- UTC小时范围，例如`18:00-18:59 UTC`；
- 北京时间范围，例如`次日02:00-02:59`；
- 北京时间是否跨日；
- 方向、策略族和策略标签；
- 7天和14天各自样本、胜率、PnL和EV；
- N12/N20样本、胜率和EV；
- 资格状态、即时状态和变化原因；
- 最近状态更新时间。

北京时间只作为展示字段，不能替换画像键或改变既有WD/WE归属。例如`WD-22`仍是UTC工作日22点画像，页面显示为北京时间次日06:00-06:59。

页面按完整画像列出所有已评估时段，并支持方向、WD/WE、资格状态和即时状态筛选。用户后续可以根据该表决定是否需要增加人工时段限制，但本次不增加人工时间白名单。

## 6. 数据加载、统计与容量治理

### 6.1 运行态加载

- 启动和币种切换时加载最近15天观察记录，覆盖14天资格窗口和边界结算。
- 即时状态只需要每个完整键最近20笔独立结果，可由最近15天记录恢复。
- 每日资格快照继续写入`daily_profile_selections`，保存7天/14天统计和联合失效次数；即时状态由最近15天观察在内存中计算，重启时重建，不把每笔结算重复写回每日快照。
- 观察和模拟订单载荷新增完整画像状态的必要字段及`runtime_config_hash`；高频信号审计使用6.5节的精简格式，不重复整份状态和配置。旧载荷字段缺失时按`WARMUP`兼容。

如果某个画像在15天内仍不足12笔即时恢复数据，N12/N20按实际样本计算并保持`WARMUP`，不会读取未来数据或用其他画像补样本。

### 6.2 观察统计读取

当前`OBSERVATION_SUMMARY_LIMIT=5000`只覆盖约17.6天，不能继续作为主要画像统计口径。修改后：

- “观察画像统计”默认展示最近14天，同时显示同一画像的7天快速窗口结果；
- API和页面支持`7天 / 14天 / 30天 / 全部数据`；
- 每个窗口按`opened_at`时间查询，不按最近固定条数截断；
- 观察明细使用SQLite的`COUNT + WHERE + ORDER BY + LIMIT/OFFSET`真实分页，不先加载最近5000条再内存分页；
- 自适应画像运行态快照不依赖通用统计接口；
- 方向脉冲仅需要N12/N16，启动恢复继续最多读取最近5000条，不影响其正确性。

“全部”读取数据库中现存的全部观察画像，不再受5000条限制。

### 6.3 线上容量依据

2026-08-17只读统计显示：

| 数据 | 行数 | SQLite占用 |
|---|---:|---:|
| `signal_audit` | 695672 | 约1.570GB |
| `observation_signals` | 8521 | 约12.4MB |
| `order_entry_snapshots` | 547 | 约9.8MB |
| `orders` | 547 | 约1.6MB |
| 整个数据库 | - | 约1.615GB |

`signal_audit`占数据库约97%，观察画像不是当前体积问题。最近14天信号审计为97892行、载荷约219MB；最近30天为224899行、载荷约398MB。SQLite表页和索引会使实际占用高于载荷大小。

### 6.4 必要指标口径

数据库容量控制以“完整复现开单判断，但不重复保存可去重数据”为原则，不以固定天数清理历史为主。

“所有影响开单的参数”定义为：当前代码在生成候选、画像提升、风险守卫、并发冷却、金额资格和最终开单分支中实际读取的每一个动态值、阈值、模式、版本和判断结果。每次新增或修改开单条件时，必须同步升级决策快照版本并补齐持久化测试；不允许出现代码读取了某个条件、数据库却无法还原该条件的情况。

每个正式候选和独立观察候选在进入准入链时生成唯一`decision_id`和`DECISION_CONTEXT_V2`输入快照。准入链执行完毕后，将有序轨迹和最终决定冻结为同一`decision_id`的`decision_outcome`。输入快照必须原样传递给信号、观察样本、实际订单入口快照和审计；订单ID、结算胜负和PnL作为关联结果单独写入，不得修改、重算或用当前配置补写历史输入。

`observation_signals`是画像筛选的主要数据。每条观察除身份、开平时间和结果外，需要保存：

1. **身份和来源**：交易对、周期、完整画像键、方向、UTC时段、策略族、策略标签、候选来源、订单席位和席位作用域；
2. **行情时间点**：用于判断的最后闭合K线时间、当前点位、入场点位、到期点位、10分钟窗口起止时间；
3. **评分和阈值**：`score`、基础`threshold`、`calculated_threshold`、`edge`、阈值窗口、阈值时段、动态阈值各调整项和最终是否达标；
4. **量价输入**：量比、基准量、当前量、放量阈值、低量阈值、10分钟涨跌幅、价格位置、窗口收盘强度、单根K线收盘强度和上下影拒绝状态；
5. **MACD**：MACD线、信号线、柱值、柱变化、柱值/变化的ATR归一化值、MACD柱阈值和变化阈值；
6. **RSI和BOLL**：RSI值及上下阈值、BOLL位置、宽度及上下阈值；
7. **指标画像**：指标画像时段、样本数、计算版本及实际命中的指标判断码；
8. **行情上下文**：10分钟偏向、行情状态、Fear & Greed值、分类、变化状态、调整值和数据更新时间；
9. **每日画像资格**：每日画像版本、7天/14天各自样本、胜率、PnL、EV、资格状态和联合失效次数；
10. **即时画像状态**：N12/N20样本、胜数、胜率、PnL、EV、`WARMUP/ACTIVE/WATCH/PAUSED`状态、状态更新时间和状态原因码；
11. **方向与波段**：1分钟波段原始/确认状态、效率、方向占比、ATR强度、确认数、批次ID、方向脉冲版本和影子结论；
12. **风险守卫**：时段、画像健康、画像退化、结算序列、波段批次、滚动优势及其他实际执行守卫的启用状态、判断状态、原因码和决定性数值；
13. **容量和冷却**：全局与方向最大在途数、判断时实际在途数、方向最近开单时间、最早可开时间、间隔分钟数和容量判断结果；
14. **金额资格**：基础金额、拟开金额、滚单启用状态、最大滚单次数、当前步骤、资格来源订单、资格版本和是否允许消费；
15. **价格结构影子**：4.5节定义的版本、模式、状态、影子判断、最近价区、ATR距离和候选来源；
16. **决策结果**：有序`decision_trace`、首个决定性阻止阶段、最终决策码、最终原因、是否正式开单和是否产生观察；订单ID、结算胜负和PnL通过同一`decision_id`关联，不属于可变的输入快照。

`decision_trace`按真实执行顺序只保存每个阶段的`stage/status/reason_code`；上述规范字段已经保存的动态值不在轨迹中重复。某个守卫存在专用且未被规范字段覆盖的输入时，才写入该阶段的紧凑`decisive_values`。

运行期不变化或低频变化的完整启动参数、画像门槛、指标周期、守卫配置、金额配置和结构参数统一规范化后生成`runtime_config_hash`，在`runtime_config_snapshots`中按哈希只保存一次。每条决策记录只保存哈希、`strategy_build_id`和各子模块版本。这样可以还原当时配置，同时避免在每分钟审计中重复整份参数。

不保存完整240根/1440根K线、全部结构价区、完整其他方向运行态或重复的`quality_score_inputs`。这些原始输入只有在某个订单的专用入口证据确实需要时才进入低频`order_entry_snapshots`；所有实际参与判断的派生值和阈值已经由`DECISION_CONTEXT_V2`覆盖。

`orders`和`order_entry_snapshots`继续保存真实模拟订单的完整入场证据，因为订单数量远低于每分钟信号。金额链路、最终守卫结论、入场K线和结算结果不精简。

### 6.5 高频信号审计精简

当前`signal_audit`每次闭合1分钟K线都保存完整`signal.to_dict()`，并重复保存全部滚动优势、结算序列、波段批次、画像退化、画像健康、方向脉冲和画像守卫配置。这些重复快照是数据库膨胀的主要原因。

修改后使用`SIGNAL_AUDIT_V2`：

1. 决策、方向、周期、时段、行情状态、分数、阈值和原因继续使用独立列；
2. JSON载荷引用同一`DECISION_CONTEXT_V2`的紧凑审计表示，保存身份、关键指标、阈值、画像状态、价格结构影子和各守卫的`status/code`及决定性数值；
3. 不再写入重复的`quality_score_inputs`，但其所有实际输入必须已由6.4节规范字段覆盖；
4. 不再逐行重复保存全部守卫配置、全部方向脉冲快照和完整每日画像候选列表；
5. 运行参数生成`runtime_config_hash`，完整配置只在新表`runtime_config_snapshots`中按哈希保存一次；审计、观察和订单只引用该哈希；
6. 正式开单、正式候选被阻止、自适应画像状态变化、守卫状态变化必须立即保存独立审计行；
7. 只有既未形成正式候选、也未形成独立观察候选的普通`WAIT/BELOW_THRESHOLD`心跳，才可在状态完全相同时按10分钟桶聚合，保存`first_at`、`last_at`、`occurrences`、最小/最大分数和最后一次指标快照；任何正式候选或观察候选必须逐笔保存完整决策上下文；
8. 观察画像仍由`observation_signals`逐笔记录，不能因为审计聚合而漏掉观察结果。

旧`signal_audit`保持可读，不重写、不迁移、不删除。统计接口同时兼容旧版单行记录和V2聚合记录，命中数量使用`occurrences`还原。

### 6.6 影子库5GiB文件峰值

正式`monitor.sqlite3`继续使用现有容量、schema和同步事务语义，不加入影子表。独立`monitor.shadow.sqlite3`上限设为5GiB：

```text
5 * 1024 * 1024 * 1024 = 5368709120 bytes
```

容量状态：

- `NORMAL`：小于4GiB，按V2正常记录；
- `WARNING`：达到4GiB，页面和`/api/state`持续告警；
- `COMPACT_ONLY`：达到4.5GiB，停止未开单影子逐分钟明细，只保存影子订单、参数版本、事件游标、缺口、每日汇总、状态变化和决定性阻止事件；
- `HARD_LIMIT`：达到5GiB后禁止普通明细写入；核心游标、订单结算、生命周期和正式策略回执继续优先复用已回收页。若SQLite实际返回容量不足，只冻结受影响参数组；正式订单库和正式开单链继续运行。

影子库根据实际`page_size`设置`PRAGMA max_page_count`，限制主数据库页数，并对新库启用`auto_vacuum=incremental`。容量状态同时报告有效占用、主库加WAL物理占用和可回收页；压缩后执行增量回收及被动WAL checkpoint。为影子参数、未结订单、结算和长期汇总预留至少512MiB，不把5GiB全部分配给未开单影子明细。可选影子审计的写入预算上限为4.5GiB减去当前影子核心表占用；达到预算后只停可选影子明细，正常情况下不影响正式行情消费、画像结算、模拟订单或Webhook。

影子订单、参数版本、每日汇总、正式策略回执、升级记录和回退记录在影子库永久保留。未形成影子订单的逐分钟影子判断保留30天，之后在同一事务中按北京时间交易日、方向、画像、决策和首个阻止原因写入`shadow_decision_rollups`，保留命中次数及分数/阈值区间；聚合成功后才清空原始大载荷。影子库容量控制依赖配置去重、未开单判断聚合和可选审计预算，不压缩或重写线上正式订单与观察记录。

## 7. 异常处理

- 即时状态计算异常时记录`last_error`并沿用该画像上一份有效状态，不得阻断其他画像和行情消费。
- 每日资格计算异常时沿用当前有效资格快照，行为与现有`FALLBACK`一致。
- 存储失败继续遵循现有存储错误路径，不先开单后补审计。
- 状态重算必须只使用`settled_at <= current_time`的观察，回放、延迟消息和重启均不得读取未来结果。
- 结构计算失败时写入`entry_structure_state=ERROR`和短错误码，并按`NEUTRAL`影子展示；不得阻断候选、画像或订单。

## 8. 测试与验收

单元测试至少覆盖：

- 7天/14天窗口边界和最近15天加载；
- 完整画像键隔离及重叠观察去重；
- 7天单次达标立即入选、任一窗口达标继续保留、双窗口连续2次失效退出；
- 旧7天生效画像首次迁移时保持资格且联合失效次数为0；
- N12/N20的`WARMUP/ACTIVE/WATCH/PAUSED`边界；
- `PAUSED`继续生成观察但不创建订单；
- `WATCH`允许10U首单、阻止第二席位和18U；
- 状态恢复迟滞；
- LONG/SHORT和不同小时完全隔离；
- UTC到北京时间展示及跨日；
- 重启恢复与旧JSON兼容；
- 7/14/30/全部观察统计窗口及SQLite真实分页；
- 观察记录包含画像筛选和归因所需的完整指标，但不重复全局配置；
- `DECISION_CONTEXT_V2`覆盖当前开单链实际读取的全部动态值、阈值、模式、版本和判断结果；
- MACD线/信号线/柱值/柱变化及ATR归一化值、RSI/BOLL实际值和当时阈值正确往返持久化；
- 同一候选在信号、观察、订单入口和审计中的`decision_context_version/runtime_config_hash`及动态值完全一致；
- 新增任一开单条件但未加入决策快照时，对应契约测试必须失败；
- `runtime_config_snapshots`按规范化哈希去重，相同配置不重复写入；
- `SIGNAL_AUDIT_V2`字段精简、运行配置哈希去重和10分钟WAIT聚合；
- 旧审计记录与V2聚合记录的兼容统计；
- 4/4.5/5GiB容量状态、512MiB核心预留和可选审计降级；
- 达到4.5GiB时可选审计停止且核心写入继续；达到5GiB时严格禁止未持久化开单；
- 因果摆动点只能在右侧确认K线闭合后出现；
- ATR价格区边界、同类聚类、独立摆动点、拒绝确认、最近价区排序、整数关口合并和距离口径；
- 预热不足、ATR无效和重启重建均保持`NEUTRAL`且不影响原流程；
- 接近、拒绝、突破待确认、突破确认、回踩、假突破和失效状态迁移；
- LONG/SHORT镜像方向映射及三类候选来源；
- 同一候选的信号、观察和订单入口使用同一结构快照；
- `SHADOW_ONLY`开启前后订单ID、方向、入场时间、金额和Webhook触发完全一致；
- 结构统计按判断、方向、来源、结构类型和画像状态正确拆分；
- 同一闭合1分钟K线形成一份不可变行情事件；相同`analyzer_hash`的参数组共享候选计算，不同分析参数独立计算，最多7个`CHALLENGER`异步消费且不改变`CHAMPION`订单、Webhook、ID和信号时间；
- 每个影子参数组拥有独立订单、方向容量、冷却、滚单资格、连亏和守卫状态，重启后从SQLite确定性恢复；
- 参数组配置规范化、哈希去重、冻结后不可修改，历史结果不得被新参数覆盖；
- 影子升级门槛、字典序排名、7日/300单前向要求、升级冷却和自动回退边界；
- 30天未开单影子明细聚合保持每日命中数、阻止原因、分数区间和控制组差异守恒；
- 即时状态异常不影响原有开单链路；
- 所有现有测试继续通过。

验收时页面必须能直接回答：程序当前选择了哪些时段、每个时段为什么入选或退出、其UTC和北京时间是什么、短期状态何时变化，以及该状态对本次订单产生了什么影响。

## 9. 发布约束

本功能在独立分支`feature/adaptive-resident-profiles`开发。完成后先推送分支并验证，不自动发布服务器、不清空模拟订单、不创建发布标签；后续发布动作由用户单独确认。

## 10. 持续影子参数优化

### 10.1 角色与不可变边界

- 每个交易对只有一个当前`CHAMPION`和最多7个并行`CHALLENGER`；
- 参数组由规范JSON、SHA-256哈希、父版本、代次、参数族、创建时间和前向生效时间唯一确定，生效后不可原地修改；
- 所有组只接收生效时间之后的行情作为前向样本，不得用后见结果补写“前向”记录；离线回放结果必须使用独立来源标记；
- `CHAMPION`继续走现有同步持久化、正式订单和Webhook链路；`CHALLENGER`只产生影子判断和影子订单；
- 影子计算通过独立CPU进程和有界跨进程队列运行。正式策略和正式SQLite事务成功后，行情协调器才以非阻塞方式提交不可变行情事件；队列饱和、子进程异常或影子存储失败只能形成缺口记录和影子状态告警，不得阻断主策略；
- 每组候选必须连续覆盖同一市场时段。存在事件缺口的日期不计入完整前向交易日和升级样本。

### 10.2 参数范围与候选生成

参数注册表将所有可优化参数分为七族：评分与动态阈值、量价和技术指标、画像资格与N12/N20、LONG/SHORT方向参数、容量冷却和守卫、价格结构、金额和滚单。固定业务事实如只做10分钟、结算规则、币种、数据源和Webhook协议只随运行配置快照记录，不进入搜索。

每一代只改变一个参数族，候选值必须来自预先注册的有限、单调、带上下界步长空间。候选生成器以当前`CHAMPION`为父版本，优先生成相邻值；单族胜出后才允许生成包含多个已验证单族变化的组合候选。系统禁止按当前结果临时扩大取值空间，禁止无限笛卡尔积，禁止同时运行超过7个候选。

候选生命周期为`PENDING -> RUNNING -> ELIGIBLE/DOMINATED/INVALID -> PROMOTED/RETIRED`。候选达到100笔前除配置非法、数据缺口或运行错误外不得提前淘汰；淘汰只停止接收新事件，不删除历史参数、订单和统计。

### 10.3 独立影子账本

每个参数组独立维护：虚拟订单ID、开平时间、方向、金额、胜负、PnL、LONG/SHORT在途数、方向最近开单时间、冷却、滚单资格、连续盈亏、滚动优势、画像健康、画像退化、波段批次和决定性阻止状态。共享观察画像事实只能读取该时点已经结算的观察，不得读取其他参数组的虚拟订单结果。

每根已闭合1分钟K线形成唯一`market_event_id`。REST补偿批次必须与正式路径一致，只调用一次批量更新并以最后一分钟推进游标，禁止把同批未来F&G状态用于中间分钟。相同`analyzer_hash`的参数组共享一份只读原始分析帧，修改评分、指标或结构参数的组使用自己的分析器结果；每组仍独立执行画像准入、守卫、容量、冷却、金额、开单和结算。影子判断至少记录参数哈希、行情事件、方向、画像、分数、阈值、是否开单、首个阻止原因、拟开金额和虚拟订单ID。影子订单结算保存入场、到期、结算点位、结果和PnL。参数组每日汇总保存总计、LONG、SHORT、首单、第二单、金额层级、胜率、EV、PnL、最大回撤、最长连赢/连亏、订单量、事件缺口和各阻止原因计数。

每个参数组的事件游标必须与该分钟的结算、状态变化、判断和开单在影子库同一事务提交，不能先推进游标。重启后从最后已提交游标继续；从未持久化的分钟只能登记缺口，不得在已经看到到期结果后补写为前向样本。影子库错误只暂停受影响参数组，不改变正式`risk_pause`或返回`STORAGE_ERROR`。

### 10.4 自动排名与升级

每天北京时间07:50对冻结参数组计算截至该时点已经结算的完整前向结果，08:00后执行生效动作。排序按下列字典序，不使用可互相补偿的加权总分：

1. 总胜率；
2. LONG与SHORT中较低方向的胜率；
3. 最差滚动3个完整交易日胜率；
4. 相对同期`CHAMPION`的胜率增量；
5. 日均订单与50笔的绝对偏差；
6. 最长连亏；
7. 最大回撤；
8. EV和总PnL；
9. 参数变化数量和参数哈希，保证完全确定性。

升级候选必须同时满足：至少7个无缺口完整前向交易日、至少300笔已结影子订单、总胜率不低于60%、LONG和SHORT分别不低于55.56%、至少5天胜率不低于55.56%、至少5天EV为正、同期胜率高于`CHAMPION`至少2个百分点、至少5天胜率高于同期`CHAMPION`、日均订单不低于`max(35, CHAMPION同期日均订单的70%)`，且最长连亏和最大回撤均不劣于`CHAMPION`。45至55笔只是目标排序区间，不得覆盖胜率优先顺序。停机期间正式预热游标若已经超过实验最小游标，旧实验标记为`SUPERSEDED_SEED_ADVANCE`并从当前正式种子生成新实验，不允许把所有参数组永久冻结在旧游标。

同一交易对7个完整交易日内最多升级一次。满足门槛的首名候选在08:00进入待生效状态；若存在正式在途订单则等待其全部结算，最长等待10分钟。影子侧只暂存生命周期请求，不能提前修改Champion。正式进程校验`symbol + generation + experiment + arm + parameter_hash`后应用策略，把不可变正式策略回执写入影子库，再返回ACK；影子侧收到`APPLIED/APPLIED_AT_DEADLINE`后才原子提交晋级或回退。拒绝、过期或存储失败均不提交Champion变化。启用影子优化器重启时只从已确认回执恢复正式策略，不以实验表中的意向状态代替正式事实。升级只替换新订单使用的冻结参数，不把影子订单转换为正式订单，也不重写既有正式订单。原`CHAMPION`保留至少14天并继续作为回退对照。

### 10.5 自动回退

新`CHAMPION`满足任一条件时回退到上一冻结版本：连续亏损达到8笔；已结至少50笔且胜率低于50%；已结至少100笔、胜率低于55.56%且落后上一版本同期至少5个百分点；或双方同期均已结至少20笔后，最大回撤达到上一版本同期的1.25倍。回撤比较增加20笔共同样本下限，避免上一版本回撤暂为0时由单笔亏损触发退回。回退只影响后续新订单，必须记录原因、证据区间、两个版本哈希和生效时间。回退后该失败版本转为`RETIRED`，不得在没有生成新哈希和重新完成前向验证时再次升级。

### 10.6 展示与输出

页面和`/api/state`只展示决策所需结果：当前`CHAMPION`版本与生效时间、运行中的候选数、当前首名候选、前向天数/订单数/总及方向胜率/日均订单/回撤/连亏、距离升级仍缺少的硬门槛、最近升级或回退记录、影子队列状态和数据缺口。完整逐参数、逐订单和逐日明细通过独立只读接口分页查询，不把大载荷塞入每秒状态刷新。
